# Photo assets

Оригиналы личных фотографий хранятся в `public/images` с именами `photo_*`.
Оптимизированные AVIF/WebP-версии для сайта имеют префиксы `couple-hero`,
`story`, `gallery` и `couple-final`. Все используемые пути находятся в
`src/data/content.js`.
